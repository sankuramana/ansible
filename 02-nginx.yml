- name: install and start nginx
  hosts: frontend
  tasks:
  - name: install nginx #jsjs
    ansible.builtin.package:
    name: nginx
    state: present

  - name: start nginx
    ansible.builtin.package:
    name: nginx
    state: started
    enabled: yes

